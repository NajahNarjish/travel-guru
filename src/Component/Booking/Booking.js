import React from 'react';

const Booking = (props) => {
    // const place = props.place;
    console.log(props.destination);
    return (
        <div>
            <h1>this is booking for </h1>

        </div>
    );
};

export default Booking;